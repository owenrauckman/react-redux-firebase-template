import React, { Fragment } from 'react';
import Login from '../../components/Login/Index';

const LoginPage = () => {
  return (
    <Fragment>
      <Login />
    </Fragment>
  );
};

export default LoginPage;
